# chatmooc 后端开发 TODO（FastAPI + RESTful，仅接口设计）

## 0. 基础工程与规范
- [ ] 初始化 FastAPI 项目结构：`app/api`、`app/schemas`、`app/services`、`app/models`、`app/tasks`、`app/core`
- [ ] 统一 API 前缀与版本：`/api/v1`
- [ ] 统一响应模型：成功/失败结构、错误码、分页结构（`page`、`page_size`、`total`）
- [ ] 设计鉴权方案（先接口层预留）：`Authorization: Bearer <token>`，用户上下文通过依赖注入获取
- [ ] OpenAPI 文档规范：为每个接口补充 `summary`、`description`、请求/响应示例
- [ ] 约定异步任务状态枚举：`PENDING`、`RUNNING`、`SUCCESS`、`FAILED`
- [ ] 技术约束落地：Agent 编排基于 LangChain/LangGraph

## 0.1 LangChain + LangGraph 架构基线（新增）
- [ ] 约定目录分层：`app/agents`（LangChain/LangGraph）、`app/tools`（Tool 定义）、`app/prompts`（提示词模板）
- [ ] 定义统一 GraphState（TypedDict/Pydantic）：`session_id`、`unit_id`、`step_id`、`messages`、`retrieval_context`、`flashcards`
- [ ] 设计 LangGraph 节点清单：`route`、`retrieve_knowledge`、`tutor_reply`、`generate_flashcards`、`web_search`、`summarize_and_plan`
- [ ] 设计条件边与终止条件：基于用户意图和工具调用结果进行分支，支持失败回退节点
- [ ] 设计 Tool 注册规范：所有工具通过 LangChain Tool 接口暴露（检索、闪卡、联网）
- [ ] 设计记忆策略：短期会话记忆（LangGraph state）+ 持久化摘要（DB）
- [ ] 设计观测与追踪：预留 LangSmith/日志埋点字段（run_id、node_name、tool_name、latency）

## 1. 领域模型与状态机（先定义 Schema）
- [ ] 定义核心实体 Schema：`Resource`、`Unit`、`LearningPath`、`LearningStep`、`Session`、`Flashcard`、`ReviewPlan`
- [ ] 资源类型枚举：`pdf`、`ppt`、`video`、`text`
- [ ] 单元状态枚举：`NOT_STARTED`、`IN_PROGRESS`、`COMPLETED`
- [ ] 学习步骤字段明确：`step_order`、`resource_ids`、`learning_objectives`、`prerequisite_step_id`
- [ ] 会话上下文字段明确：当前 `unit_id`、当前 `step_id`、当前知识库上下文版本
- [ ] 统一审计字段：`id`、`created_at`、`updated_at`、`created_by`

## 2. 学习资源管理接口（上传、解析、组织、索引）
- [ ] 设计资源上传接口：`POST /resources/upload`（支持文件元信息、上传凭证或直传）
- [ ] 设计资源查询接口：`GET /resources`（分页、按类型/关键词/单元过滤）
- [ ] 设计资源详情接口：`GET /resources/{resource_id}`
- [ ] 设计资源更新接口：`PATCH /resources/{resource_id}`（标题、标签、描述）
- [ ] 设计资源删除接口：`DELETE /resources/{resource_id}`
- [ ] 设计解析任务创建接口：`POST /resources/{resource_id}/parse`（触发异步解析）
- [ ] 设计解析任务状态接口：`GET /tasks/{task_id}`（解析/切分/向量化状态）
- [ ] 设计资源解析结果接口：`GET /resources/{resource_id}/analysis`（关键词、摘要、切片统计）
- [ ] 设计 Milvus 索引写入触发接口：`POST /resources/{resource_id}/index`
- [ ] 设计资源与单元绑定接口：`POST /units/{unit_id}/resources`、`DELETE /units/{unit_id}/resources/{resource_id}`

## 3. 单元（Unit）管理接口
- [ ] 设计单元创建接口：`POST /units`
- [ ] 设计单元列表接口：`GET /units`（按状态筛选）
- [ ] 设计单元详情接口：`GET /units/{unit_id}`（含资源列表、学习进度）
- [ ] 设计单元更新接口：`PATCH /units/{unit_id}`（名称、描述、目标）
- [ ] 设计单元状态变更接口：`PATCH /units/{unit_id}/status`
- [ ] 设计单元删除接口：`DELETE /units/{unit_id}`

## 4. 学习路径规划接口（Workflow 生成）
- [ ] 设计学习路径生成接口：`POST /learning-paths/generate`
  - 输入：`unit_id` 或 `unit_ids`、期望步数范围（2~5）、学习偏好
  - 输出：路径草案（步骤串行、每步资源集合、每步学习目标）
- [ ] 设计学习路径保存接口：`POST /learning-paths`
- [ ] 设计学习路径列表接口：`GET /learning-paths`
- [ ] 设计学习路径详情接口：`GET /learning-paths/{path_id}`（返回步骤树）
- [ ] 设计学习步骤详情接口：`GET /learning-paths/{path_id}/steps/{step_id}`
- [ ] 设计学习路径更新接口：`PATCH /learning-paths/{path_id}`（重排步骤、调整目标）
- [ ] 设计学习路径删除接口：`DELETE /learning-paths/{path_id}`
- [ ] 设计下一步计划接口：`POST /learning-paths/{path_id}/next-plan`（根据本次学习总结生成下一步）
- [ ] 明确该模块的实现约束：路径生成 Workflow 由 LangGraph 图执行，路径生成提示与解析由 LangChain Chain 承载

## 5. 交互学习 Agent 接口（仅 API 契约）
- [ ] 设计学习会话创建接口：`POST /sessions`（绑定 `unit_id` + `step_id`）
- [ ] 设计学习会话详情接口：`GET /sessions/{session_id}`
- [ ] 设计会话消息接口：`POST /sessions/{session_id}/messages`（用户输入 -> Agent 回复）
- [ ] 设计会话历史接口：`GET /sessions/{session_id}/messages`
- [ ] 明确该模块的实现约束：会话消息处理由 LangGraph 驱动，具体节点内部调用 LangChain Agent/Chain

## 6. Agent 工具设计（知识检索/闪卡/联网）仅使用 `@tool` 装饰器注册，不提供 REST API
- [ ] 定义知识库检索工具：`retrieval_search_tool`
  - 输入：`query`、`unit_id`、`top_k`
  - 输出：命中文档片段、相关度分数、来源资源
- [ ] 定义复习闪卡生成工具：`flashcards_generate_tool`
  - 输入：`unit_id` 或 `session_id`、题量、难度
  - 输出：问答卡片列表（正面/背面/标签/来源）
- [ ] 定义联网检索工具：`web_search_tool`
  - 输入：`query`
  - 输出：搜索结果、摘要、引用链接
- [ ] 定义统一 Tool I/O Schema（Pydantic）：所有 Tool 入参与返回值可校验、可追踪
- [ ] 定义 Tool 异常语义：超时、空结果、外部服务失败，统一为可被 LangGraph 节点处理的错误对象
- [ ] 定义 Tool 注册与装配规范：仅通过 LangChain `@tool` 暴露，并在 LangGraph 节点中按需调用

; ## 7. 闪卡与复习计划接口
; - [ ] 设计闪卡列表接口：`GET /flashcards`（按单元/标签/掌握度筛选）
; - [ ] 设计闪卡详情接口：`GET /flashcards/{flashcard_id}`
; - [ ] 设计闪卡更新接口：`PATCH /flashcards/{flashcard_id}`（内容修订、标签）
; - [ ] 设计闪卡作答接口：`POST /flashcards/{flashcard_id}/reviews`（记录是否掌握、耗时）
; - [ ] 设计复习计划生成接口：`POST /review-plans/generate`
; - [ ] 设计复习计划查询接口：`GET /review-plans/{plan_id}`
; - [ ] 设计定时推送配置接口：`PUT /review-push/settings`
; - [ ] 设计推送任务查询接口：`GET /review-push/jobs`

; ## 8. 学习过程总结与进度接口
; - [ ] 设计学习总结提交接口：`POST /sessions/{session_id}/summary`
; - [ ] 设计学习总结查询接口：`GET /sessions/{session_id}/summary`
; - [ ] 设计单元进度接口：`GET /units/{unit_id}/progress`
; - [ ] 设计全局学习看板接口：`GET /dashboard/learning-overview`

; ## 9. 非功能性接口设计任务
; - [ ] 为关键接口定义幂等策略（尤其 `generate`、`upload`、`parse`）
; - [ ] 统一限流与配额字段设计（按用户、按工具）
; - [ ] 统一可观测性字段：`request_id`、`trace_id`、`latency_ms`
; - [ ] 设计错误码清单文档（参数错误、状态冲突、外部依赖失败等）
; - [ ] 设计 API 测试清单（契约测试、错误路径测试、鉴权测试）

## 10. 里程碑（接口设计阶段）
- [ ] M1：完成全部 Pydantic 请求/响应模型定义
- [ ] M2：完成 LangChain + LangGraph 架构基线与 GraphState 设计
- [ ] M3：完成资源、单元、学习路径三大核心模块的 API 路由草案
- [ ] M5：完成 Agent 会话 API 契约与 Tool 契约（含 LangGraph 节点映射）
- [ ] M6：输出 OpenAPI 文档 + Postman/Apifox 集合


uv pip install -U transformers FlagEmbedding sentence-transformers